#!/bin/sh
# systemLinux: firmware update daemon (run by fwupd.service; nixpkgs' D-Bus activation file is not on
# the system bus's search path, so it is kept running instead of being started on demand).
exec /nix/var/goget/profiles/system/current/libexec/fwupd/fwupd

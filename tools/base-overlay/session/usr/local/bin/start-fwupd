#!/bin/sh
# systemLinux: firmware update daemon (started and supervised by lenine; normally D-Bus-activated,
# but lenine has no D-Bus activation mechanism, so it is just kept running instead).
exec /nix/var/goget/profiles/system/current/libexec/fwupd/fwupd
